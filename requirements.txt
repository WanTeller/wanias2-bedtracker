Django==5.1.4

# Production (also fine to have installed locally):
gunicorn==23.0.0            # WSGI server on Linux hosts (Render etc.)
waitress==3.0.2             # WSGI server that also runs on Windows (self-hosting)
whitenoise==6.8.2           # serves static files from the app process
dj-database-url==2.3.0      # reads the DATABASE_URL the host/Neon provides
psycopg[binary]==3.2.3      # PostgreSQL driver

# The local backup script (backup/) uses only the Python standard library.
